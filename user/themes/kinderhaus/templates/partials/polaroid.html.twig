<figure class="polaroid {{ classes }}">
  {% set name = image.basename|replace('_', ' ') %}
  {{ image.lightbox().cropZoom(250, 250).html(name, name, 'polaroid__image') }}
  <figcaption class="polaroid__caption">{{ name }}</figcaption>
</figure>
